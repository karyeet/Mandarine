/*

Changes nickname of bot to currently playing song name

If nickname returns true, nickname wil be song title
If nickname returns false, error occured with changing nickname

*/

/*
function nickname() {
	message.guild.me.setNickName(message.content.replace(song, ""));
}

module.exports = nickname;
*/